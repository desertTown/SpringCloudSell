spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      label: master
      profile: test

# F 版本是默认不开启的， 需要手动开启。  但是不开启好像也是可以正常访问到对应的服务信息的
feign:
  hystrix:
    enabled=true: