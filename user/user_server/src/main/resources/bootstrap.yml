server:
  port: 8992


spring:
  application:
    name: user
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config
      label: kubernetes
      profile: test

eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:8765/eureka/

logging:
  level:
    org.springframework.cloud.bus: debug
